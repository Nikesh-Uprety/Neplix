import { AdminPlaceholder } from "@/components/admin/AdminPlaceholder";

export default function AdminOrdersNewPage() {
  return (
    <AdminPlaceholder
      title="Create Order"
      subtitle="Manual order desk and checkout links"
      description="New-order workflow route now exists and is ready to be connected with POS/cart/order-link APIs."
    />
  );
}
