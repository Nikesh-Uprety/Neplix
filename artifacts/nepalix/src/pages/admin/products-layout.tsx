import { AdminPlaceholder } from "@/components/admin/AdminPlaceholder";

export default function AdminProductsLayoutPage() {
  return (
    <AdminPlaceholder
      title="Product Layout"
      subtitle="Storefront product page layout settings"
      description="Product layout route is available for RR parity and can be connected to layout config APIs."
    />
  );
}
